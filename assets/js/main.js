window.addEventListener('load', function(){
  var slider = tns({
    container: '.slideshow',
    controls: false,
    items: 1,
    autoplay: true,
    autoplayButtonOutput: false,
    autoplayTimeout: 10000
  })
})
